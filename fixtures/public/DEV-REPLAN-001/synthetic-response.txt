The migration cannot advance as a single cutover while the v1 consumer remains authoritative. Introduce a bounded compatibility phase that emits or translates both versions at the ownership boundary, verify the legacy consumer and v2 consumers independently, then remove v1 only after evidence shows no remaining v1 dependency. Each phase should have an explicit artifact identity and rollback point so a failed gate blocks only the dependent transition.
