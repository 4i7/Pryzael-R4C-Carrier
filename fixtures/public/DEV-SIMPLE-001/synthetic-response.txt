The cache violates its key-canonicalization invariant: write and read paths must derive the same canonical key. Canonicalize the lookup key with the same lower-casing function used on write, rather than adding a fallback scan. A focused regression should store under a mixed-case key and retrieve through a differently cased spelling; it should fail before the repair and return the stored value after it.
