import test from 'node:test';
import assert from 'node:assert/strict';
import { mkdtempSync, mkdirSync, rmSync, writeFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { createHash } from 'node:crypto';
import { buildFrozenManifestFromMaterial, assertDeterministicCurrentRender } from '../src/frozen-manifest.mjs';
import { gitBlobSha1, loadFrozenMaterial, verifyFrozenIdentity, verifyInventory } from '../src/frozen-source.mjs';
import { renderAbsent, renderCurrent, rendererImplementationSha256 } from '../src/condition-renderer.mjs';
import { generateSlots } from '../src/slots.mjs';
import { buildSubjectEnvelope } from '../src/subject-envelope.mjs';
import { captureResponse, verifyResponseCapture } from '../src/response-capture.mjs';
import { buildJudgeEnvelope } from '../src/judge-envelope.mjs';
import { validateCompleteness } from '../src/completeness.mjs';

const identity={repository:'repo',sourceCommit:'commit',sourceTree:'tree',canonicalSkillsTree:'skills',pluginVersion:'1.0.0'}; const bytes=Buffer.from('exact semantic bytes\n'); const entryBase={path:'skills/a/SKILL.md',blobSha1:gitBlobSha1(bytes),byteCount:bytes.length}; const manifest=buildFrozenManifestFromMaterial({actualIdentity:identity,expectedIdentity:identity,materialByPath:new Map([[entryBase.path,bytes]]),expectedPathBlobs:[entryBase],rendererImplementationSha256:rendererImplementationSha256()}); const current=renderCurrent({materialByPath:new Map([[entryBase.path,bytes]]),manifest}); const absent=renderAbsent(); const attestation={temporaryChat:true,outsideProjects:true,personalization:false,nativePryzael:false,pluginPryzael:false,mcpPryzael:false,candidateMaterial:false,sessionReused:false,model:'public-test-model',product:'public-test-product',ordinaryTools:[]}; const [noSlot,currentSlot]=generateSlots({qualificationId:'q',taskIds:['task'],trialIndices:[0]});
for(const field of ['sourceCommit','sourceTree','canonicalSkillsTree']) test(`fails closed on wrong ${field}`,()=>assert.throws(()=>verifyFrozenIdentity({...identity,[field]:`wrong-${field}`},identity),/mismatch/));
test('fails closed on wrong or missing Git blob evidence',()=>{const exactEntry={...entryBase,sha256:createHash('sha256').update(bytes).digest('hex')};assert.throws(()=>verifyInventory(new Map([[entryBase.path,bytes]]),{entries:[{...exactEntry,blobSha1:'0'.repeat(40)}]}),/blobSha1 mismatch/);assert.throws(()=>verifyInventory(new Map(),{entries:[exactEntry]}),/missing path/);});
test('fails closed on changed file bytes',()=>assert.throws(()=>renderCurrent({materialByPath:new Map([[entryBase.path,Buffer.from('exact semantic bytes\r\n')]]),manifest}),/(blobSha1|byteCount|sha256) mismatch/));
test('fails closed on changed rendered digest',()=>assert.throws(()=>renderCurrent({materialByPath:new Map([[entryBase.path,bytes]]),manifest:{...manifest,conditionRenderSha256:'0'.repeat(64)}}),/condition render digest mismatch/));
test('fails closed on renderer nondeterminism',()=>{let sequence=0;assert.throws(()=>assertDeterministicCurrentRender({materialByPath:new Map(),manifest:{},renderCurrentFn:()=>{const conditionBytes=Buffer.from(`nondeterministic-${sequence++}`);return{conditionBytes,conditionRenderSha256:createHash('sha256').update(conditionBytes).digest('hex')}}}),/renderer nondeterminism/);});
test('fails closed on missing, duplicate, or unexpected slots',()=>{const slots=generateSlots({qualificationId:'q',taskIds:['task'],trialIndices:[0]});const record=slot=>({...slot,artifactIdentity:slot.conditionId==='CURRENT_PRYZAEL'?'artifact':'NONE',conditionRenderSha256:slot.conditionId==='CURRENT_PRYZAEL'?current.conditionRenderSha256:absent.conditionRenderSha256,responseSha256:'a'.repeat(64),hostProfileDigest:'h'.repeat(64),judgeResult:{responseSha256:'a'.repeat(64),trialResult:{}}});const expectedIdentity={currentArtifactId:'artifact',currentRenderSha256:current.conditionRenderSha256,absentRenderSha256:absent.conditionRenderSha256};assert.throws(()=>validateCompleteness({expectedSlots:slots,evidence:[record(slots[0])],expectedIdentity}),/missing slot/);assert.throws(()=>validateCompleteness({expectedSlots:slots,evidence:[record(slots[0]),record(slots[0]),record(slots[1])],expectedIdentity}),/duplicate slot evidence/);assert.throws(()=>validateCompleteness({expectedSlots:slots,evidence:[record(slots[0]),record({...slots[1],slotId:'unexpected'})],expectedIdentity}),/unexpected slot/);});
test('fails closed on paired host-profile mismatch',()=>{const slots=generateSlots({qualificationId:'q',taskIds:['task'],trialIndices:[0]});const evidence=slots.map((slot,index)=>({...slot,artifactIdentity:slot.conditionId==='CURRENT_PRYZAEL'?'artifact':'NONE',conditionRenderSha256:slot.conditionId==='CURRENT_PRYZAEL'?current.conditionRenderSha256:absent.conditionRenderSha256,responseSha256:'b'.repeat(64),hostProfileDigest:index===0?'host-a':'host-b',judgeResult:{responseSha256:'b'.repeat(64),trialResult:{}}}));assert.throws(()=>validateCompleteness({expectedSlots:slots,evidence,expectedIdentity:{currentArtifactId:'artifact',currentRenderSha256:current.conditionRenderSha256,absentRenderSha256:absent.conditionRenderSha256}}),/host-profile mismatch/);});
test('fails closed on contaminated session attestation',()=>assert.throws(()=>buildSubjectEnvelope({slot:noSlot,condition:absent,taskPrompt:'task',authorityEnvelope:'ordinary authority',attestation:{...attestation,sessionReused:true}}),/sessionReused mismatch/));
test('fails closed on altered response bytes',()=>{const capture=captureResponse(Buffer.from('response\r\n'));assert.throws(()=>verifyResponseCapture(Buffer.from('response\n'),capture),/response (byte count|digest) mismatch/);});
test('fails closed on condition identity leakage into Judge envelope',()=>{const response=captureResponse(Buffer.from('ordinary response'));assert.throws(()=>buildJudgeEnvelope({judgeBlindId:`jb-${'a'.repeat(64)}`,responseBytes:response.responseBytes,responseCapture:response,judgeAuthority:{conditionId:'anything'}}),/condition identity leakage/);});
test('fails closed when CURRENT material is missing',()=>assert.throws(()=>buildSubjectEnvelope({slot:currentSlot,condition:absent,taskPrompt:'task',authorityEnvelope:'ordinary authority',attestation}),/CURRENT material missing/));
test('fails closed when NO contains semantic material',()=>assert.throws(()=>buildSubjectEnvelope({slot:noSlot,condition:current,taskPrompt:'task',authorityEnvelope:'ordinary authority',attestation}),/semantic material present in NO_PRYZAEL/));
test('fails closed on mechanically detectable candidate/R5 material contamination',()=>{const root=mkdtempSync(join(tmpdir(),'r4c-candidate-'));try{mkdirSync(join(root,'skills','a','references'),{recursive:true});writeFileSync(join(root,'skills','a','SKILL.md'),bytes);writeFileSync(join(root,'skills','a','references','r5-candidate.md'),'candidate semantics');assert.throws(()=>loadFrozenMaterial(root,{entries:[manifest.entries[0]]}),/unexpected model-visible path/);}finally{rmSync(root,{recursive:true,force:true});}});
